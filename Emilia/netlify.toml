[build]
  base    = "Emilia"
  # ADD THIS LINE: This forces a clean install of all dependencies before building.
  install = "npm install"
  command = "npm run build"
  publish = "Emilia/dist" 

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200